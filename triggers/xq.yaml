match: (xinqiu|xing?qui)

interaction:
  content: |
    > ${ $MATCH[1] }
    xingqiu*

channels:
  whitelist:
    - ${ CHANNELS.QPH }
    - ${ CHANNELS.QPH_BOT_SPAM }
    - ${ CHANNELS.COMMAND_SPAM }
