match: ${ TRIGGER_PREFIX }calc\s(.*)$

interaction:
  content: >-
    **Result:** ${
      const result = mathjsEvaluate($MATCH[1]);
      (typeof result === 'string' || typeof result === 'number') ? result : 'Invalid Expression';
    }
