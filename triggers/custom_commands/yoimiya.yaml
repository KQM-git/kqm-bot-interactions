interaction: characters/pyro/yoimiya
description: "Opens KQM's resources on Yoimiya"

match: ${ TRIGGER_PREFIX }yoimiya(?:\s|$)
channels:
  whitelist:
    - "${ CHANNELS.TASUKETE_PLAYGROUND }"
    - "${ CHANNELS.TASUKETE_SPAM }"