interaction: characters/hydro/barbara/main
description: "Opens KQM's resouces on Barbara"

match: ${ TRIGGER_PREFIX }barbara(?:\s|$)
channels:
  whitelist:    
  - "${ CHANNELS.TASUKETE_PLAYGROUND }"
  - "${ CHANNELS.TASUKETE_SPAM }"