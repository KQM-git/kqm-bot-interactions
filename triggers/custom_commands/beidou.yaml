interaction: characters/electro/beidou
description: "Opens KQM's resources on Beidou"

match: ${ TRIGGER_PREFIX }beidou(?:\s|$)
channels:
  whitelist:    
  - "${ CHANNELS.TASUKETE_PLAYGROUND }"
  - "${ CHANNELS.TASUKETE_SPAM }"