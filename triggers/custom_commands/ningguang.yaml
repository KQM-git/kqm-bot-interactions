interaction: characters/geo/ningguang/main
description: "Opens KQM's resouces on Ningguang"

match: ${ TRIGGER_PREFIX }ningguang(?:\s|$)
channels:
  whitelist:
    - "${ CHANNELS.TASUKETE_PLAYGROUND }"
    - "${ CHANNELS.TASUKETE_SPAM }"