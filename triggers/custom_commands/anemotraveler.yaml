interaction: characters/anemo/anemotraveler
description: "Opens KQM's resources on Anemo MC"

match: ${ TRIGGER_PREFIX }(?:anemotravell?er|amc)(?:\s|$)
channels:
  whitelist:
    - "${ CHANNELS.TASUKETE_PLAYGROUND }"
    - "${ CHANNELS.TASUKETE_SPAM }"