interaction: characters/pyro/amber
description: "Opens KQM's resources on Amber"
match: ${ TRIGGER_PREFIX }amber(?:\s|$)
channels:
  whitelist:    
  - "${ CHANNELS.TASUKETE_PLAYGROUND }"
  - "${ CHANNELS.TASUKETE_SPAM }"
  - "${ CHANNELS.TASUKETE_SPAM }"