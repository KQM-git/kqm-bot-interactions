interaction: characters/pyro/klee
description: "Opens KQM's resources on Klee"

match: ${ TRIGGER_PREFIX }klee(?:\s|$)
channels:
  whitelist:
    - "${ CHANNELS.TASUKETE_PLAYGROUND }"
    - "${ CHANNELS.TASUKETE_SPAM }"