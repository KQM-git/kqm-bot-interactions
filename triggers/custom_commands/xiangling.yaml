interaction: characters/pyro/xiangling
description: "Opens KQM's Resources on Xiangling"

match: ${ TRIGGER_PREFIX }xiangling(?:\s|$)
channels:
  whitelist:
    - "${ CHANNELS.TASUKETE_PLAYGROUND }"
    - "${ CHANNELS.TASUKETE_SPAM }"