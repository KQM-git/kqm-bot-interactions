interaction: characters/pyro/bennett/main
description: "Opens KQM's resources on Bennett"

match: ${ TRIGGER_PREFIX }bennett(?:\s|$)
channels:
  whitelist:    
  - "${ CHANNELS.TASUKETE_PLAYGROUND }"
  - "${ CHANNELS.TASUKETE_SPAM }"