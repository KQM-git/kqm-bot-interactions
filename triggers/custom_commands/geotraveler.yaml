interaction: characters/geo/geotraveler/main
description: "Opens KQM's resouces on Geo MC"

match: ${ TRIGGER_PREFIX }geotraveler(?:\s|$)
channels:
  whitelist:    
  - "${ CHANNELS.TASUKETE_PLAYGROUND }"
  - "${ CHANNELS.TASUKETE_SPAM }"