interaction: characters/anemo/venti
description: "Opens KQM's resouces on Venti"

match: ${ TRIGGER_PREFIX }venti(?:\s|$)
channels:
  whitelist:
    - "${ CHANNELS.TASUKETE_PLAYGROUND }"
    - "${ CHANNELS.TASUKETE_SPAM }"