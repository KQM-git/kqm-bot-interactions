content: ${ const maxcv = mathjsEvaluate ( !$OPTIONS.CURRENTLEVEL && $OPTIONS.CURRENTCV > 7.8) ? ($OPTIONS.CURRENTCV + (20 - $OPTIONS.CURRENTLEVEL + $OPTIONS.CURRENTLEVEL % 4 - 1) / 4 * 7.8) : ($OPTIONS.CURRENTCV+ (20 - $OPTIONS.CURRENTLEVEL + $OPTIONS.CURRENTLEVEL % 4) / 4 * 7.8) }

options:
  currentCV: 
  required: true 
  description: your artifacts current CV
  type: number

  currentLevel: 
  required: true 
  description: your mum
  type: number
