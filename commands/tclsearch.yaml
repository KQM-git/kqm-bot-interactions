description: Search TCL

interaction:
  content: "**Search: **${@OPTIONS.QUERY} ${@ENCODED.@OPTIONS.QUERY}"
  buttons:
    - label: RESULTS
      style: LINK
      url: "https://library.keqingmains.com/?q="
      
options:
  query:
    type: string
    required: true
    description: search query
