description: Surprise Command
interaction:
  embeds:
    - title: DEEZ
      description: NUTZ
      footer:
        text: "Command run by ${  @USER.USERNAME  }"
        icon_url: "${  @USER.AVATAR  }"
      timestamp: "${  @DATE.JSON_STRING  }"
      url: "${  @USER.AVATAR  }"
      image:
        url: "${  @USER.AVATAR  }"
    - url: "${  @USER.AVATAR  }"
      image:
        url: "${  @USER.AVATAR  }"
    - url: "${  @USER.AVATAR  }"
      image:
        url: "${  @USER.AVATAR  }"
    - url: "${  @USER.AVATAR  }"
      image:
        url: "${  @USER.AVATAR  }"
        
ephemeral: ! ${ @OPTIONS.DISPLAY }
options:
  display:
    type: boolean
    description: display this command for everyone
    required: false
