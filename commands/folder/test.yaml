description: Surprise Command
interaction:
  embeds:
    - title: DEEZ
      description: NUTZ
      footer:
        text: "Command run by {{ @USER.USERNAME }}"
        icon_url: "{{ @USER.AVATAR }}"
      timestamp: "{{ @DATE.JSON_STRING }}"
